import React from 'react';
import { View, Text } from 'react-native';

export default function InventoryScreen() {
  return (
    <View style={{ flex: 1, padding: 16 }}>
      <Text>Inventory (later: items, equipped, magic effects, bonuses)</Text>
    </View>
  );
}
